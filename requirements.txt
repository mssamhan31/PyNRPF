# Notebook + config + download
pyyaml>=6,<7
requests>=2.31,<3

# Data + parquet
pandas>=2.1,<3
numpy>=1.26,<3
pyarrow>=14,<20

# ML (needed later, but not optional)
scikit-learn>=1.4,<2
xgboost>=2,<3
holidays>=0.40,<1

# Plotting
matplotlib>=3.7,<4
